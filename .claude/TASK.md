The OpenClaw browser usage tool works pretty well. Look in the parent directory for OpenClaw and see how it is implemented. Then let's add a similar tool for Mindroom—not just for the OpenClaw compatibility layer, although it should definitely be used there, but also as a separate browser usage tool. Whatever the name is that OpenClaw uses, it should have the same name and implementation.
