{{- if eq (.Values.workerBackend | default "static_runner") "kubernetes" }}
apiVersion: v1
kind: Secret
metadata:
  name: mindroom-worker-auth-{{ .Values.customer }}
  namespace: mindroom-instances
  labels:
    app: mindroom
    customer: {{ .Values.customer | quote }}
type: Opaque
{{- end }}
