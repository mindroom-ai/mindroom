# Source Duplication Audit Agent Prompt

You are one of many low-reasoning explorer agents auditing MindRoom source duplication.
Your primary file is exactly one Python module under `./src`.
You may inspect any other file under `./src`, but do not edit production code.

Write your report to the exact report path provided in your task.
If you cannot write the file, return the full report in your final answer.

## Goal

For the primary file, identify duplicated behavior elsewhere in `./src`.
Duplication includes literal duplicate code, near-duplicate helper logic, repeated parsing or validation, repeated IO or API wrapping, repeated Matrix/message/config transformations, and repeated lifecycle/error-handling flows.

## Required Coverage Contract

You will receive an exact AST symbol checklist for the primary file.
Your report must include a fenced `coverage-tsv` block with one row for every required symbol and no missing symbols.
Use this exact header:

```coverage-tsv
qualname	kind	lines	status	search_terms	candidates_checked
```

Rules:

- `qualname`, `kind`, and `lines` must match the provided checklist.
- `status` must be one of `duplicate-found`, `related-only`, `none-found`, or `not-a-behavior-symbol`.
- `search_terms` must briefly state what you searched or compared for that symbol.
- `candidates_checked` must list candidate file paths and line numbers, or `none`.
- If the module has no symbols, include a single row for `MODULE_LEVEL`.

## Required Report Shape

1. Summary: top duplication candidates for this primary file, or `No meaningful duplication found`.
2. Coverage TSV: exactly one row per required symbol.
3. Findings: each real duplication with file/line references, why behavior is duplicated, and differences to preserve.
4. Proposed generalization: minimal helper/module location, or `No refactor recommended`.
5. Risk/tests: behavior risks and tests that would need attention.

Keep findings factual and conservative.
Do not propose broad architecture changes unless the duplication is clearly active and repeated.


## This Agent's Assignment

Primary file: `src/mindroom/tool_system/dependencies.py`

Report path: `docs/duplication-audit/reports-src/0275-src__mindroom__tool_system__dependencies.py-aabfcbab.md`

Module index: `0275`

Line count: `255`

Required symbol count: `20`

## Required Symbol Checklist

- `_pip_name_to_import`	function	lines 51-61
- `_normalize_extra_name`	function	lines 64-66
- `check_deps_installed`	function	lines 69-75
- `auto_install_enabled`	function	lines 78-81
- `_has_lockfile`	function	lines 84-86
- `_available_optional_extras`	function	lines 90-102
- `_resolve_optional_extra_name`	function	lines 105-115
- `_is_uv_tool_install`	function	lines 118-120
- `_in_virtualenv`	function	lines 123-124
- `_get_current_uv_tool_extras`	function	lines 127-136
- `_install_via_uv_tool`	function	lines 139-150
- `_current_python_has_module`	function	lines 153-155
- `install_command_for_current_python`	function	lines 158-172
- `_install_via_uv_sync`	function	lines 175-189
- `_install_in_environment`	function	lines 192-199
- `_install_optional_extras`	function	lines 202-218
- `auto_install_optional_extra`	function	lines 221-228
- `auto_install_tool_extra`	function	lines 231-233
- `ensure_optional_deps`	function	lines 236-244
- `ensure_tool_deps`	function	lines 247-255
