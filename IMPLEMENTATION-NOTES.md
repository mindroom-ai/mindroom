Fix round 1 completed for PR #809: OAuth tool construction now defers connection prompts to invocation and preserves structured sandbox responses, callback persistence strips unsafe ID-token data while preserving refresh tokens, state and connect token storage is safe for workers and concurrent processes, redirects honor public origins, non-OAuth auth providers still use ordinary credential checks, popup cancellation is surfaced correctly, Calendar defaults to read-only scopes with write methods gated by `allow_update`, and dead helpers and merge paths were removed. Validation passed with `uv sync --all-extras`, `nix-shell --run 'uv run pytest tests/ -x -n auto --no-cov -v'`, `uv run pre-commit run --all-files`, `uv run tach check --dependencies --interfaces`, `cd frontend && npx vitest run`, `cd frontend && npm run build`, and the OAuth provider smoke check returning `['google_calendar', 'google_drive', 'google_gmail', 'google_sheets']`.
