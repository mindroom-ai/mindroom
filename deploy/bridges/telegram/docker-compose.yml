version: '3.8'

services:
  mautrix-telegram:
    image: dock.mau.dev/mautrix/telegram:latest
    restart: unless-stopped
    volumes:
      - ./data:/data
    ports:
      - "29317:29317"
    networks:
      - bridge-network

networks:
  bridge-network:
    name: matrix-bridges
    external: true
